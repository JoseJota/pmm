package com.example.josluc.controlpersonas;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.widget.TextView;

/**
 * Created by josluc on 27/10/17.
 */

public class Pantalla2 extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_pantalla2); /*
        elSaludo=(TextView)findViewById(R.id.todoObjeto);

        Bundle miBundleRecoger = getIntent().getExtras();

        Titular t = (Titular) miBundleRecoger.getSerializable("CLAVEobjeto");
        elSaludo.setText(t.toString()); */
    }
}
